<Window x:Class="Kesmai.WorldForge.UI.Windows.ComponentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="clr-namespace:Kesmai.WorldForge.UI"
        mc:Ignorable="d"
        Title="Select Component"
        Height="600"
        Width="800"
        Background="#FFDDDDDD"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ui:ComponentCategoryTreeView Grid.Column="0"
                                          Margin="0,0,10,0"
                                          SelectedItemChanged="OnCategorySelected"
                                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                          ScrollViewer.VerticalScrollBarVisibility="Auto"/>

            <ui:ComponentGrid Grid.Column="1"
                              ItemsSource="{Binding SelectedCategory.Components, Source={StaticResource componentPalette}}"
                              SelectedItem="{Binding SelectedProvider, Source={StaticResource componentPalette}}"
                              MouseDoubleClick="OnComponentDoubleClick"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        </Grid>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Select"
                    Width="120"
                    Margin="0,0,0,0"
                    Padding="12,6"
                    Click="OnSelectClicked"/>
        </StackPanel>
    </Grid>
</Window>
