<UserControl x:Class="Kesmai.WorldForge.UI.Documents.RegionSpawnDocument"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:worldForge="clr-namespace:Kesmai.WorldForge"
             xmlns:scripting="clr-namespace:Kesmai.WorldForge.Scripting"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="600"
             FontFamily="Tahoma" FontSize="14">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="3"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="3"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="3"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <TabControl Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" x:Name="_scriptsTabControl"
                    ItemsSource="{Binding ActiveContent.Scripts, Source={StaticResource applicationPresenter}}" 
                    Margin="5" MinHeight="100">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <CheckBox IsChecked="{Binding IsEnabled}" Margin="5,2" />
                        <Image Source="../../Resources/CSharp-Script.png" Width="16" Height="16" />
                        <TextBlock Text="{Binding Name}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="True">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                            <Setter Property="Foreground" Value="Gray"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <scripting:ScriptEditor Signature="{Binding Signature}"
                                            Header="{Binding Header}"
                                            Body="{Binding Body, Mode=TwoWay}"
                                            Footer="{Binding Footer}" 
                                           
                                            IsEnabled="{Binding IsEnabled}" />
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
        
        <GridSplitter Grid.Row="1" Grid.ColumnSpan="2" Height="3" HorizontalAlignment="Stretch" />
        
        <DataGrid Grid.Row="2" Margin="5" Height="300"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding ActiveContent.Entries, Source={StaticResource applicationPresenter}}">
            <DataGrid.Resources>
                <CollectionViewSource x:Key="EntitiesCollection" Source="{Binding Segment.Entities, Source={StaticResource applicationPresenter}}" />
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridComboBoxColumn MinWidth="200" Header="Entity" 
                                        ItemsSource="{Binding Source={StaticResource EntitiesCollection}}"
                                        SelectedItemBinding="{Binding Entity, UpdateSourceTrigger=PropertyChanged}"></DataGridComboBoxColumn>

                <DataGridTextColumn MinWidth="70" Header="Size" Binding="{Binding Size, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                <DataGridTextColumn MinWidth="70" Header="Minimum" Binding="{Binding Minimum, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                <DataGridTextColumn MinWidth="70" Header="Maximum" Binding="{Binding Maximum, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <GridSplitter Grid.Row="3" Height="3" HorizontalAlignment="Stretch" />
        
        <GroupBox Header="Inclusions" Grid.Row="4" Grid.Column="0"
                  MinHeight="100" MaxHeight="200">
            <DataGrid AutoGenerateColumns="False" 
                      ItemsSource="{Binding ActiveContent.Inclusions, Source={StaticResource applicationPresenter}}"
                      CanUserAddRows="true" CanUserDeleteRows="true"
                      FontFamily="Tahoma" FontSize="12">
                <DataGrid.Columns>
                    <DataGridTextColumn MinWidth="70" Header="Left" Binding="{Binding Left}"></DataGridTextColumn>
                    <DataGridTextColumn MinWidth="70" Header="Top" Binding="{Binding Top}"></DataGridTextColumn>
                    <DataGridTextColumn MinWidth="70" Header="Right" Binding="{Binding Right}"></DataGridTextColumn>
                    <DataGridTextColumn MinWidth="70" Header="Bottom" Binding="{Binding Bottom}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        
        <GridSplitter Grid.Row="5" Height="3" HorizontalAlignment="Stretch" />

        <GroupBox Header="Exclusions" Grid.Row="6" 
                  MinHeight="100" MaxHeight="200">
            <DataGrid AutoGenerateColumns="False" 
                      ItemsSource="{Binding ActiveContent.Exclusions, Source={StaticResource applicationPresenter}}"
                      CanUserAddRows="true" CanUserDeleteRows="true"
                      FontFamily="Tahoma" FontSize="12">
                <DataGrid.Columns>
                    <DataGridTextColumn MinWidth="70" Header="Left" Binding="{Binding Left}"></DataGridTextColumn>
                    <DataGridTextColumn MinWidth="70" Header="Top" Binding="{Binding Top}"></DataGridTextColumn>
                    <DataGridTextColumn MinWidth="70" Header="Right" Binding="{Binding Right}"></DataGridTextColumn>
                    <DataGridTextColumn MinWidth="70" Header="Bottom" Binding="{Binding Bottom}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        
        <worldForge:SpawnsPresentationTarget Grid.Column="1" Grid.Row="1" Grid.RowSpan="6" x:Name="_presenter" Margin="2" />
    </Grid>
</UserControl>